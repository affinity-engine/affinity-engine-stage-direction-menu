import Menu from 'ember-theater-director-menu/ember-theater/director/directions/menu';
import { module, test } from 'qunit';

module('Unit | Ember Theater Direction | menu');

// Replace this with your real tests.
test('it works', function(assert) {
  var menu = Menu.create();
  assert.ok(menu);
});
